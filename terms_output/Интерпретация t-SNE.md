# Интерпретация t-SNE

## определение

Расстояния между кластерами не имеют количественного смысла, важна только относительная близость точек внутри кластеров. t-SNE не сохраняет глобальные расстояния и масштабы.


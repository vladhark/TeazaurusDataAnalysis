# Фильтрация через виджеты (updatemenus / buttons) в Plotly

## определение

Элементы управления, позволяющие динамически показывать/скрывать слои на графике. updatemenus с buttons; visible массива трасс управляется через args.


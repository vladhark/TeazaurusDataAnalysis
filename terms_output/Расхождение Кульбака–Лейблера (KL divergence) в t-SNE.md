# Расхождение Кульбака–Лейблера (KL divergence) в t-SNE

## определение

Мера различия между распределением вероятностей соседства в исходном и низкоразмерном пространстве. t-SNE минимизирует KL(P||Q). KL(P||Q) = Σ_{i≠j} p_{ij} log(p_{ij}/q_{ij}).


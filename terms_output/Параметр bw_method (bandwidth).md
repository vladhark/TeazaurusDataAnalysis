# Параметр bw_method (bandwidth)

## определение

Регулирует «гладкость» оценки: малые значения → высокая детализация (шум), большие → излишнее сглаживание. bw_method='scott' (оптимально по умолчанию), bw_method=0.5 (для малых выборок).


# KS-тест (Kolmogorov–Smirnov)

## определение

Непараметрический тест равенства распределений двух непрерывных выборок. scipy.stats.ks_2samp(). H₀: распределения одинаковы (дрейфа нет). p-value < 0.05 – дрейф значим.


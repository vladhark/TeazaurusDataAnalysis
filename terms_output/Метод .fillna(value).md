# Метод .fillna(value)

## определение

Заменяет пропуски указанным значением (константой, строкой, результатом вычислений). Можно заполнять медианой, модой, средним или значением из другого столбца.


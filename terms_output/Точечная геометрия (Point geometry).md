# Точечная геометрия (Point geometry)

## определение

Объект shapely.geometry.Point, представляющий одну географическую точку (например, место посадки/высадки). Создаётся через gpd.points_from_xy(x, y, crs=...).


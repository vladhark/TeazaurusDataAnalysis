```dataviewjs
const folderPath = "Анализ Данных термины/terms_output";
dv.header(1, "📖 Глоссарий терминов");
const pages = dv.pages(`"${folderPath}"`).sort(p => p.file.name, 'asc');
for (let page of pages) {
    dv.header(3, `[[${page.file.name}]]`);
    dv.paragraph(`> [!info]- Определение\n> ${page.file.name} — это термин, определение которого находится в его заметке.`);
}
```
